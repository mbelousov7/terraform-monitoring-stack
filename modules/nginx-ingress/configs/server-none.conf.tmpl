%{ for server in server_list ~}
server {
 listen ${ container_port };
 #resolver ${ resolver } valid=10s;
 server_name ${ server.name }.local;
 location / {
   set $upstream http://${ server.name }.${ namespace }.svc.cluster.local:9090;
   proxy_pass $upstream;
 }
}
%{ endfor ~}
